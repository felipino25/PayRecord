# PAYRECORD — Dependencias de producción
# Instalación:  pip install -r requirements.txt
# Python 3.12.x

Django==5.2.17              # framework web (LTS)
mysqlclient==2.2.8          # driver MySQL
django-environ==0.14.0      # lectura de variables desde .env
django-crispy-forms==2.7    # renderizado de formularios
crispy-bootstrap5==2026.3   # plantillas Bootstrap 5 para crispy-forms

# --- Solo necesarias al desplegar (no estorban en local) ---
whitenoise==6.12.0          # sirve los archivos estáticos sin servidor aparte
gunicorn==26.2.0            # servidor WSGI que usa Azure App Service (Linux)

# --- Dependencias transitivas (instaladas automáticamente) ---
# asgiref, sqlparse, tzdata
